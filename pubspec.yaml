name: cenoyam
description: Certainly Not Yandex Music.

publish_to: 'none'

version: 0.1.0

environment:
  sdk: ">=2.13.0 <3.0.0"

dependencies:
  audioplayers:
  convert:
  crypto:
  flutter:
    sdk: flutter
  flutter_bloc:
  freezed_annotation:
  get_it:
  http:
  injectable:
  json_annotation:
  provider:

dev_dependencies:
  build_runner:
  flutter_lints:
  flutter_test:
    sdk: flutter
  freezed:
  injectable_generator:
  json_serializable:


flutter:
  uses-material-design: true
